// 2.
const num = 10;
console.log(num);

// 3.
const first = 5;
const second = 10;

console.log(first + second);
console.log(first - second);
console.log(first * second);
console.log(first / second);

// 4.
const a = 3;
const b = 10;
const result = a + b;

// 5.
const bar = 20;
const qwerty = 5;
const res = bar - qwerty - 10;
const end = res - qwerty;

// 6.
const str = "Hello, Palmo";
alert(str);

// 7.
const name = "Лёша";
const age = 39;
alert(`Я ${name}, мне ${age}.`);

// 8.
const secInHour = 60 * 60;
const secInDay = secInHour * 24;
const secInMonth = secInDay * 30;

const times = [secInHour, secInDay, secInMonth];
times.forEach((n) => console.log(n));

// 9.
const number = 4;
const numberSquare = number ** 2;

// 10.
const d = 8;
const n = d ** 2;
const x = d ** 3;
