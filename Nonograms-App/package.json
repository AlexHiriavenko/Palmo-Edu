{
  "name": "coffee-house",
  "private": false,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npx vite build && npx gh-pages -d dist -e nonograms"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "gh-pages": "^6.1.0"
  },
  "dependencies": {
    "sass": "^1.69.5",
    "vite-plugin-html-inject": "^1.0.4"
  }
}
